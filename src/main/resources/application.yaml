ktor:
    deployment:
        port: 8085
        connectionGroupSize: 4
        workerGroupSize: 4
        callGroupSize: 8
    application:
        modules:
            - coliver.ApplicationKt.module


storage:
    user: $USER
    password: $PASSWORD
    jdbcURL: $JDBC_URL
